- Prefers to work in discussion/planning mode first — wants the project's structure and design explained and confirmed before any scaffolding or code is written. Confidence: 0.9
- Works on Windows (PowerShell, `C:\` paths). Confidence: 0.7
- Prefers explanations written in simple, plain words and bullet points rather than dense prose — including very short one-sentence summaries when confirming understanding. Confidence: 0.85
- When planning a project, wants to know up front what will be tested and what will be observed (expected outcomes). Confidence: 0.8
- Values minimal, terse artifacts and fast execution — dislikes long text files, wasted time, and unnecessary token usage. Confidence: 0.7
- Prefers run outputs/reports to be versioned and preserved rather than overwritten — incremental numbered files (e.g. SUMMARY1.md, SUMMARY2.md) that accumulate across runs. Confidence: 0.7
- Wants the exact literal command to copy-paste rather than the conceptual command form — provide the ready-to-paste line (e.g. `.\watch.ps1` when already in the folder's PowerShell prompt) instead of the full `powershell -NoProfile -File ...` invocation. Confidence: 0.7
- When run instructions involve multiple commands, wants the execution sequence spelled out explicitly — whether to run them one at a time in order (and wait for each to finish) versus combining them — rather than just a code block of commands. Confidence: 0.6
- Wants to read and understand the actual code rather than treat it as a black box — asks how to open/read the files, which file does what, which specific line/instruction triggers each behavior, why a behavior is designed a certain way (e.g. one fix per loop rather than all at once), and how the code changes across each step (initial broken state → each fix → final). Confidence: 0.85
- After a project is built and tested, wants a walkthrough of how to run and test it himself, plus the results and findings — a post-completion run/test guide with the observed outcomes and lessons. Confidence: 0.8
- When pasting terminal/command output or a GitHub/web page, wants a section-by-section explanation of what each part means (what happened and what each line or UI element shows), including calling out misleading signals (e.g. a green checkmark only means the workflow ran, not that the task succeeded). Confidence: 0.7
- Appreciates honest, demystified explanations of how something actually works — wants to be told plainly what was pre-scripted/simulated versus genuinely "figured out" (e.g. "no AI repaired the symbols; the fix was pre-written and the test command is the only judge"), and welcomes having misconceptions corrected directly. Confidence: 0.6
- Prefers clean, terse per-iteration loop output — one short line per beat summarizing state (e.g. "error-N repaired, X more remaining : test says PASS/FAIL") instead of verbose raw error/test dumps, and wants confusing output wording revised. Confidence: 0.7
- Prefers append-style timestamped progress logs (e.g. task-done.txt with "DONE-N at <timestamp>" lines) alongside per-run numbered summary files. Confidence: 0.8
- Wants reports/logs to be concrete and reader-facing, not vague — each step should name the actual thing changed (e.g. "fixed 'add' (was 'a - b', now 'a + b')") and list exactly what still remains broken, rather than abstract wording like "error-1 repaired". Confidence: 0.7
- Organizes each project/experiment in its own dedicated subfolder under a shared project root (e.g. C:\Projects\eng_loop\<project>), rather than reusing a single working directory. Confidence: 0.75
- Wants each run/tool invocation to print a self-contained, state-transparent report — explicitly stating what was already known before the run (history-before), how many new items were found, and what action was taken — so the reader never has to infer what the script actually did from a vague one-liner. Confidence: 0.7
- Prefers using a throwaway git repo for autonomous loop/agent work — never pointing early loops at real work that matters. Confidence: 0.7
- Prefers setting a safety limit (max tries, max minutes, or max spend) before letting any loop/agent run on its own. Confidence: 0.7
- Values a checker/reviewer that can actually reject bad work — treats a checker that approves everything as no checker at all, and expects it to be tightened if it passes a deliberately planted bad case. Confidence: 0.7
- When a run's result is unclear, wants a concrete step-by-step trace of the actual values computed (e.g. `multiply(4,3)` → `4*3 = 12` → matches expected 12 → PASS, vs `4-3 = 1` → FAIL), not just a high-level flow description. Confidence: 0.6
- Asks for plain-language definitions of unfamiliar terms (e.g. "what does PR mean?") and wants the underlying concepts/tooling (repo, branch, commit, PR) explained from the very beginning in short bullet points rather than assuming prior knowledge. Confidence: 0.7
- Has a GitHub account and, when a real external integration is available, prefers doing the real thing (e.g. GitHub PR/event workflow) over a local simulation — wants to actually connect it rather than settle for a simulated shape. Confidence: 0.6
- Keeps two GitHub accounts (a main/Yahoo one and a Gmail one) and prefers routing throwaway/test repos and PRs through the secondary Gmail account so the main account stays untouched. Confidence: 0.75
- Prefers explanations delivered slowly and one step at a time ("slowly slowly tell me") rather than a large batch of everything at once. Confidence: 0.6
- When he can't find something in a UI (e.g. "I don't see the branch"), wants a concrete pointer to exactly where to look — which page, which button/dropdown to click, and what he'll see — plus an optional local command (e.g. `git branch`) to independently confirm, rather than only a conceptual explanation. Confidence: 0.55
- When he spots something in a tool/UI that contradicts what was described (e.g. the wrong branch marked "Default"), he expects it verified against ground truth (e.g. `gh repo view --json defaultBranchRef`) and actually fixed, not just explained away. Confidence: 0.6
- Wants the auto-vs-manual distinction called out explicitly — when a tool/step happened automatically versus was done deliberately by the assistant (e.g. "does creating two branches auto-open a PR, or did you run `gh pr create`?"), and what the actual thing to observe is versus the steps that were just setup. Even within a single workflow, wants the manual trigger separated from the automatic consequence (e.g. the PR was created manually, but the review that followed fired automatically). Confidence: 0.7
- When reviewing already-built project work, wants an honest compliance check against the assignment's "Done when"/requirements — confirming what is correct AND flagging what is wrong, untidy, or missing (e.g. leftover uncommitted changes), not just validating that it runs. Confidence: 0.8
- Prefers to include a tool/dependency (e.g. git) only when the assignment genuinely requires it — asks "do we actually need X here or not" before deciding, and will add or remove it based on need rather than including it by default. Confidence: 0.6
- When asked to "fix it", wants the assistant to actually perform the fix/cleanup immediately and then explain what it did afterward — action first, then a walkthrough/summary of the changes made. Confidence: 0.75

